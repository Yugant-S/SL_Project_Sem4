<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="40dp"
    android:background="#FFFFFF">

    <!-- Illustration Image -->
    <ImageView
        android:id="@+id/mainIllustration"
        android:layout_width="302dp"
        android:layout_height="300dp"
        android:contentDescription="Illustration"
        android:src="@drawable/img_1" />

    <!-- Title Text -->
    <TextView
        android:id="@+id/mainTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Track your finanaces"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#5350B2"
        android:layout_marginTop="25dp"/>

    <!-- Subtitle Text -->
    <TextView
        android:id="@+id/mainSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Picturize your income and expenses with inituitive, easy to read charts."
        android:textSize="18sp"
        android:textColor="#7A7A7A"
        android:textAlignment="center"
        android:layout_marginTop="20dp"/>

    <!-- Login Button -->
    <Button
        android:id="@+id/loginBtn"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Login"
        android:textSize="18dp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#5350B2"
        android:layout_marginTop="40dp"/>

    <!-- Sign-up Option -->
    <TextView
        android:id="@+id/signupText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Don't have an account? Sign up"
        android:textSize="16sp"
        android:textColor="#000000"
        android:layout_marginTop="20dp"
        android:textStyle="bold"/>
</LinearLayout>
